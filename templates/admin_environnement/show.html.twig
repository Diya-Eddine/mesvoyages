{% extends 'base.html.twig' %}

{% block title %}Environnement{% endblock %}

{% block body %}
    <h1>Environnement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ environnement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ environnement.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_environnement_index') }}">back to list</a>

    <a href="{{ path('app_admin_environnement_edit', {'id': environnement.id}) }}">edit</a>

    {{ include('admin_environnement/_delete_form.html.twig') }}
{% endblock %}
